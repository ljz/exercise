void quicksort(A, p,r)
{
  if p<r:
    q = partition(A, p, r);
    quicksort(A, p, q-1);
    quicksort(A, q+1, r);
}

void partition(A, p, r)
{
  key = A[p];
 for(int i = p+1; i<r; i++)
  {
	if :wq
 
  }

}

